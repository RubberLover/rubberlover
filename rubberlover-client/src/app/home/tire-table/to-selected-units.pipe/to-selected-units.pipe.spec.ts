import { ToSelectedUnitsPipe } from './to-selected-units.pipe';

describe('ToSelectedUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToSelectedUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
